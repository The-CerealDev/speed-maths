\documentclass[11pt,a4paper]{article}
\input{../../shared/preamble}
\SpeedHeader{Logic}{3}

\begin{document}

\SpeedTitleBlock{Daily Logic Drill \#3}{David Akinyele-Aje}
\SpeedMeta{Contrapositive and contradiction}{contrapositive, proof by contradiction, sufficient conditions}
\noindent\textit{New toolkit today: Direct Proof $\cdot$ Proof by Contrapositive.}

\vspace{2pt}
\noindent\textit{Attempt each question before checking answers. Time yourself. No calculators.}

\section*{Section A \quad Rapid Recognition \hfill{\normalfont\small($\leq$15\,s each)}}
% ══════════════════════════════════════════════════════════════════════════════

\noindent\textit{Short, immediate proofs --- direct and by contrapositive, paired so you see both routes to the same fact.}

\begin{enumerate}

\item Prove directly: ``If $n$ is even, then $n^2$ is even.''

\item Prove directly: ``If $n$ is odd, then $n^2$ is odd.''

\item Prove directly: ``If $a$ and $b$ are both even, then $a+b$ is even.''

\item State the contrapositive of ``If $n$ is a multiple of $6$, then $n$ is even.'' Prove the contrapositive directly.

\item Prove directly: ``If $a$ and $b$ are both odd, then $ab$ is odd.''

\item True or false: ``To prove `if $P$ then $Q$' by contrapositive, you should assume $Q$ is false and show $P$ is false.''

\item Prove by contrapositive: ``If $n^2$ is odd, then $n$ is odd.''

\item True or false: ``A proof by contrapositive is a different technique from proving the converse.''

\item Prove directly: ``If $x>1$, then $x^2>1$.''

\item Prove by contrapositive: ``If $x^2\leq1$, then $x\leq1$.''

\end{enumerate}

% ══════════════════════════════════════════════════════════════════════════════
\section*{Section B \quad Manipulation Drills \hfill{\normalfont\small($\leq$50\,s each)}}
% ══════════════════════════════════════════════════════════════════════════════

\noindent\textit{Slightly longer proofs, and judgement calls about which route is more natural.}

\begin{enumerate}

\item Prove directly: ``If $n$ is a multiple of $4$, then $n^2$ is a multiple of $16$.''

\item Prove by contrapositive: ``If $n^2$ is not a multiple of $3$, then $n$ is not a multiple of $3$.''

\item Which proof route is more natural for ``If $n^2$ is even, then $n$ is even'' --- direct or contrapositive? Prove it using that route.

\item Prove directly: ``If $a>b>0$, then $a^2>b^2$.''

\item Prove by contrapositive: ``If $x^2-4x+3\neq0$, then $x\neq1$ and $x\neq3$.''

\item Prove directly: ``If $n$ is a multiple of $3$, then $n^3$ is a multiple of $27$.''

\item State and prove the contrapositive of ``If $xy$ is irrational, then $x$ is irrational or $y$ is irrational.''

\item Prove directly: ``If $n$ ends in the digit $5$, then $n^2$ ends in the digit $5$.''

\item Prove by contrapositive: ``If $n$ is not a multiple of $4$, then $n^2$ is not a multiple of $16$.''

\item True or false: ``Every proof by contrapositive can be rewritten as a direct proof of the original statement.''

\end{enumerate}

% ══════════════════════════════════════════════════════════════════════════════
\section*{Section C \quad Substitution \& Structure \hfill{\normalfont\small($\leq$90\,s each)}}
% ══════════════════════════════════════════════════════════════════════════════

\noindent\textit{Multi-step proofs, and your first ``which condition is sufficient'' MCQ.}

\begin{enumerate}

\item For non-zero real numbers $x,y$, which one of the following is sufficient to conclude that $x<y$?
\begin{itemize}
\item[A)] $x^2<y^2$
\item[B)] $y^2<x^2$
\item[C)] $|x|<|y|$
\item[D)] $|y|<|x|$
\item[E)] $x^5<y^5$
\item[F)] $y^5<x^5$
\end{itemize}

\item Prove by contrapositive: ``If $n^3$ is even, then $n$ is even.''

\item Prove directly: ``If $x,y>0$ and $x^2>y^2$, then $x>y$.''

\item Prove directly: ``If $n$ is a multiple of $3$ but not a multiple of $9$, then $n^2$ is a multiple of $9$ but not a multiple of $81$.''

\item Prove by contrapositive: ``If $x$ and $y$ are real numbers with $xy$ irrational, then $x\neq0$ and $y\neq0$.''

\item Prove by contrapositive: ``If $n$ is an integer such that $2n^2+1$ is a multiple of $3$, then $n$ is not a multiple of $3$.''

\item Prove directly: ``If $n$ is a positive integer that is not a multiple of $3$, then $n^2\equiv1\pmod3$.''

\item True or false, with justification: ``A statement with no counterexamples found after checking $100$ cases has therefore been proven true by direct proof.''

\end{enumerate}

% ══════════════════════════════════════════════════════════════════════════════
\section*{Section D \quad Challenge \hfill{\normalfont\small(TMUA / SMC difficulty)}}
% ══════════════════════════════════════════════════════════════════════════════

\noindent\textit{Insight over computation. Each question has a short, elegant solution --- find it.}

\begin{enumerate}

\item A region is defined by the inequalities $x+y>10$ and $x-y>2$. Consider the three statements: I. $x>6$ II. $y>4$ III. $(x+y)(x-y)>20$. Which of the above statements is/are true for every point in the region?
\begin{itemize}
\item[A)] none
\item[B)] I only
\item[C)] II only
\item[D)] III only
\item[E)] I and II only
\item[F)] I and III only
\item[G)] II and III only
\item[H)] I, II and III
\end{itemize}

\item Prove by contradiction: ``There is no pair of positive integers $(m,n)$ with $m^2-n^2=2018$.''

\item Prove directly: ``If $n>4$ is such that both $n-1$ and $n+1$ are prime, then $n$ is a multiple of $6$.''

\item Prove directly: ``If $x$ and $y$ are positive reals with $x\neq y$, then $\dfrac{x+y}{2}>\sqrt{xy}$.''

\item Statement $T$: ``If $n$ is a positive integer such that $n^2$ is a multiple of $6$, then $n$ is a multiple of $6$.'' (a) Is $T$ true? Prove or disprove. (b) Contrast with the fact that ``$n^2$ is a multiple of $4$'' does \emph{not} force ``$n$ is a multiple of $4$'' (witnessed by $n=6$). What property of $6$, that $4$ lacks, makes $T$ true?

\end{enumerate}

\SpeedClosing{``Speed comes from recognition, not from rushing.
Every shortcut is a pattern you have internalised.''}

\end{document}
